def is_it_prime(number):
    if number <= 1:
        return False
    for i in range(2, number):
        if number % i == 0:
            return False
    return True
def write_primes(number):
    for i in range(2, number+1):
        if is_it_prime(i):
            print(i)
